## Pawjai Daily PR Audit — 2026-07-27

### ⚠️ ACTION REQUIRED (PRs to main/master — do not merge without your review)

| Repo | PR | Link | Risk | Key Issues | Recommendation |
|------|----|------|------|------------|----------------|
| pawjai | #63 docs(chat-write): add planning artifacts for chat-write feature | `feature/chat-write-docs` → `main` | Low | Docs-only PR. ADR notes idempotency risk (double-tap confirm) is delegated to FE — this is documented as a known trade-off. Branch has 20+ commits including submodule bumps alongside the docs change. | **Approve** — well-written ADR and specs, no code changes, low risk. Consider squashing unrelated commits before merge. |

### ✅ Auto-merged (staging PRs — build passed)
_(none — no staging-targeted PRs found)_

### ❌ Build Failed (not merged — needs your attention)
_(none)_

### 📭 Clean repos
- stateless-x/pawjai-fe — no open PRs
- stateless-x/pawjai-be — no open PRs
- stateless-x/pawjai-admin — no open PRs
- stateless-x/pawjai-ios — no open PRs

---

**Audit notes:**
- GitHub REST API was blocked by session proxy (requires `add_repo` tool available only in GitHub Actions context). PR detection used `git ls-remote refs/pull/*/merge` as fallback. PR titles/base-branch metadata retrieved via git show. This issue was posted via git push to a temporary branch.
- PR #63 is targeting `main` directly. Per audit rules, not merged automatically.

---
_Claude Code Scheduled Agent_
